// Composition point for optional cloud modules.
// The open-source core ships with an empty list; a downstream distribution
// can register additional NestJS modules here without touching core files.
export const CLOUD_MODULES = [];
